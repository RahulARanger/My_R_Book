# it asks for the "select the mirror" if first time
# select nearby mirror for downloading quickly
install.packages("ggplot2") # for single package

install.packages(c("reshape2", "dplyr")) # for multiple packages

# Austria Mirror is the main mirror, all new packages first reach this mirror

install.packages("devtools")  # for downloading packages from other sites
