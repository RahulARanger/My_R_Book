# in r, it's recommended to use <- over = for assignment operations.

sample <- 69  # variables
print(sample)

die <- 1:6
print(die)

print(ls())  # returns the names of the variables used (even after this line)

now <- "check"

print(now)
